{{- if .Values.itTools.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.itTools.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.itTools.ports.http.name }}
    port: {{ .Values.itTools.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.itTools.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
