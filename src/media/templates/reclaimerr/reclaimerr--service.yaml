apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.reclaimerr.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.reclaimerr.label }}{{ .Values.k8s.names.pods.suffix }}
  ports:
  - name: {{ .Values.reclaimerr.ports.http.name }}
    port: {{ .Values.reclaimerr.ports.http.targetPort }}
    targetPort: {{ .Values.reclaimerr.ports.http.targetPort }}
