bazarr:
  apiKey: 1b229188fe9e4488a2333c2135b1a058
  host: ${k8s.names.urls.prefix}${bazarr.label}${k8s.names.urls.suffix}.${traefik.domain}
  image: linuxserver/bazarr:latest
  label: bazarr
  ports:
    http:
      name: bazarr-http
      targetPort: 6767
  pvc:
    app: ${storage.defaultVolumes.apps}
    media: ${storage.defaultVolumes.media}
  replicasNb: 1
