# This manifest was generated by flux. DO NOT EDIT -- except spec.ref below.
#
# local deploys from `dev`; prod deploys from `main`. Merging dev -> main IS
# the prod promotion. `flux bootstrap` regenerates this file with whatever
# branch it was invoked for AND strips comments like this one -- if that
# happens, re-apply `branch: dev` here. That exact comment-stripping is how
# the previous ref decision got lost and later misread as drift, so the
# authoritative record lives in docs/branching.md, which Flux never rewrites.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: dev
  secretRef:
    name: flux-system
  url: ssh://git@github.com/platypod/stack
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/local
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
