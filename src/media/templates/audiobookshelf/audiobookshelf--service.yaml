{{- if .Values.audiobookshelf.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.audiobookshelf.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.audiobookshelf.ports.http.name }}
    port: {{ .Values.audiobookshelf.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.audiobookshelf.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
