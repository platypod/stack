apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.observability.opentelemetryCollector.collectors.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.observability.opentelemetryCollector.collectors.ports.grpc.name }}
    port: {{ .Values.observability.opentelemetryCollector.collectors.ports.grpc.targetPort }}
    targetPort: {{ .Values.observability.opentelemetryCollector.collectors.ports.grpc.targetPort }}
  - protocol: TCP
    name: {{ .Values.observability.opentelemetryCollector.collectors.ports.http.name }}
    port: {{ .Values.observability.opentelemetryCollector.collectors.ports.http.targetPort }}
    targetPort: {{ .Values.observability.opentelemetryCollector.collectors.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.observability.opentelemetryCollector.collectors.label }}{{ .Values.k8s.names.pods.suffix }}
