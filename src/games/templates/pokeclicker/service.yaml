apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.pokeclicker.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.pokeclicker.ports.http.name }}
    port: {{ .Values.pokeclicker.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.pokeclicker.label }}{{ .Values.k8s.names.pods.suffix }}
