{{- if .Values.itTools.enable }}
{{- $pod := print .Values.k8s.names.pods.prefix .Values.itTools.label .Values.k8s.names.pods.suffix }}

apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ $pod }}
  labels:
    app: {{ $pod }}

spec:
  replicas: {{ .Values.itTools.replicasNb }}
  selector:
    matchLabels:
      app: {{ $pod }}
  template:
    metadata:
      labels:
        app: {{ $pod }}
    spec:
      containers:
      - name: {{ .Values.k8s.names.containers.prefix }}{{ .Values.itTools.label }}{{ .Values.k8s.names.containers.suffix }}
        image: {{ .Values.itTools.image }}
        resources:
          {{- toYaml .Values.itTools.resources | nindent 10 }}
        ports:
        - name: {{ .Values.itTools.ports.http.name }}
          containerPort: {{ .Values.itTools.ports.http.targetPort }}
{{- end }}
