{{- if .Values.sphaze.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.sphaze.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.sphaze.ports.http.name }}
    port: {{ .Values.sphaze.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.sphaze.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
