jellyfin:
  apiKey: 6d65fa79175e41128fb097d3e2b3eec9
  baseUrl:
  host: ${k8s.names.urls.prefix}${jellyfin.label}${k8s.names.urls.suffix}.${traefik.domain}
  credentials:
    username: guest
    password: ""
  label: jellyfin-local
  names:
    service: ${k8s.names.services.prefix}${jellyfin.label}${k8s.names.services.suffix}
  proxy:
    host: host.docker.internal
    port: 8096
