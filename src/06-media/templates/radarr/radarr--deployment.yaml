{{- $appVolume := "app-volume" }}
{{- $configMap := "config-map" }}
{{- $mediaVolume := "media-volume" }}
{{- $pod := print .Values.k8s.names.pods.prefix .Values.radarr.label .Values.k8s.names.pods.suffix }}

apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ $pod }}
  labels:
    app: {{ $pod }}

spec:
  replicas: {{ .Values.radarr.replicasNb }}
  selector:
    matchLabels:
      app: {{ $pod }}
  template:
    metadata:
      labels:
        app: {{ $pod }}
    spec:
      containers:
      - name: {{ .Values.k8s.names.containers.prefix }}{{ .Values.radarr.label }}{{ .Values.k8s.names.containers.suffix }}
        image: {{ .Values.radarr.image }}
        ports:
        - name: {{ .Values.radarr.ports.http.name }}
          containerPort: {{ .Values.radarr.ports.http.targetPort }}
        env:
        - name: PUID
          value: "{{ .Values.media.system.userId }}"
        - name: PGID
          value: "{{ .Values.media.system.groupId }}"
        - name: UMASK
          value: "{{ .Values.media.system.umask }}"
        volumeMounts:
        - mountPath: /config
          name: {{ $appVolume }}
          subPath: radarr/config
        - mountPath: /config/config.xml
          name: {{ $configMap }}
          subPath: config.xml
        - mountPath: {{ .Values.media.system.data.path }}
          name: {{ $mediaVolume }}
      volumes:
      - name: {{ $mediaVolume }}
        persistentVolumeClaim:
          claimName: {{ .Values.radarr.pvc.media }}
      - name: {{ $appVolume }}
        persistentVolumeClaim:
          claimName: {{ .Values.radarr.pvc.app }}
      - name: {{ $configMap }}
        configMap:
          name: {{ .Values.k8s.names.configMaps.prefix }}{{ .Values.radarr.label }}{{ .Values.k8s.names.configMaps.suffix }}
          defaultMode: 0666
