apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.authelia.names.service }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.authelia.ports.http.name }}
    port: {{ .Values.authelia.ports.http.targetPort }}
  selector:
    app: {{ .Values.authelia.names.pod }}
