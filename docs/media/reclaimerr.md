# reclaimerr (media)

Disk-reclaim helper for the *arr stack (manages/reclaims space from completed or
stale downloads).

- **Image:** `ghcr.io/jessielw/reclaimerr:latest` (**unpinned**).
- **Auth:** on the Authelia **`bypass`** list.
- **Setup:** [reclaimerr-setup](reclaimerr-setup-job.md) seeds its config.
