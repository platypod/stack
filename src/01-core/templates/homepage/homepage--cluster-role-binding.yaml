apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding

metadata:
  name: {{ .Release.Namespace }}--{{ .Values.homepage.names.roleBinding }}

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ .Release.Namespace }}--{{ .Values.homepage.names.role }}

subjects:
  - kind: ServiceAccount
    name: {{ .Values.homepage.names.serviceAccount }}
    namespace: {{ .Release.Namespace }}
