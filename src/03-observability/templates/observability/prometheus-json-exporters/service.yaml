apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.observability.prometheusJsonExporter.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.observability.prometheusJsonExporter.ports.http.name }}
    port: {{ .Values.observability.prometheusJsonExporter.ports.http.targetPort }}
    targetPort: {{ .Values.observability.prometheusJsonExporter.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.observability.prometheusJsonExporter.label }}{{ .Values.k8s.names.pods.suffix }}
