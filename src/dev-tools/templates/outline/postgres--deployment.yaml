{{- if .Values.outline.enable }}
{{- $pod := print .Values.k8s.names.pods.prefix (tpl .Values.outline.database.label .) .Values.k8s.names.pods.suffix }}

apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ $pod }}
  labels:
    app: {{ $pod }}

spec:
  replicas: {{ .Values.outline.database.replicasNb }}
  selector:
    matchLabels:
      app: {{ $pod }}
  template:
    metadata:
      labels:
        app: {{ $pod }}
    spec:
      containers:
      - name: {{ .Values.k8s.names.containers.prefix }}{{ tpl .Values.outline.database.label . }}{{ .Values.k8s.names.containers.suffix }}
        image: {{ .Values.outline.database.image }}
        resources:
          {{- toYaml .Values.outline.database.resources | nindent 10 }}
        ports:
        - name: {{ .Values.outline.database.ports.http.name }}
          containerPort: {{ .Values.outline.database.ports.http.targetPort }}
        env:
        - name: POSTGRES_DB
          value: "{{ tpl .Values.outline.database.name . }}"
        - name: POSTGRES_USER
          value: "{{ tpl .Values.outline.database.credentials.username . }}"
        - name: POSTGRES_PASSWORD
          value: "{{ tpl .Values.outline.database.credentials.password . }}"
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: data
          subPath: {{ tpl .Values.outline.database.label . }}
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: {{ tpl .Values.outline.database.pvc . }}
{{- end }}
