# rommapp (games)

RomM — a ROM library manager/web UI.

- **Image:** `rommapp/romm:4.8.1-slim` (pinned).
- **Database:** [mariadb](mariadb.md) (`rommapp-db`).
- **Exposure:** host via Traefik. OIDC via Authelia (part of the OIDC-expansion set).
- **Storage:** ROM library on the NFS `media` share.
