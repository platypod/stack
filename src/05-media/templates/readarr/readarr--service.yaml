apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.readarr.names.service }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.readarr.ports.http.name }}
    port: {{ .Values.readarr.ports.http.targetPort }}
  selector:
    app: {{ .Values.readarr.names.pod }}
