# bookstack (dev-tools)

Wiki / documentation platform.

- **Image:** `lscr.io/linuxserver/bookstack:latest` (**unpinned** — LinuxServer image).
- **Database:** [mysql](mysql.md) (`mysql:9`, `bookstack-db`).
- **Seeding:** [bookstack-role-seed-job](bookstack-role-seed-job.md) sets up roles
  (e.g. OIDC group → BookStack role mapping).
- **Exposure:** host via Traefik.
