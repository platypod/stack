# postgres (media)

The media module's database — backs [jellyseerr](jellyseerr.md).

- **Image:** `postgres:17` (pinned).
- **Scope:** internal only (ClusterIP); no ingress.

Stock Postgres; credentials from the consuming app's values.
