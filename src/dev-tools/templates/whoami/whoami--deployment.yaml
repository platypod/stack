{{- if .Values.whoami.enable }}
{{- $pod := print .Values.k8s.names.pods.prefix .Values.whoami.label .Values.k8s.names.pods.suffix }}

apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ $pod }}
  labels:
    app: {{ $pod }}

spec:
  replicas: {{ .Values.whoami.replicasNb }}
  selector:
    matchLabels:
      app: {{ $pod }}
  template:
    metadata:
      labels:
        app: {{ $pod }}
    spec:
      containers:
      - name: {{ .Values.k8s.names.containers.prefix }}{{ .Values.whoami.label }}{{ .Values.k8s.names.containers.suffix }}
        image: {{ .Values.whoami.image }}
        resources:
          {{- toYaml .Values.whoami.resources | nindent 10 }}
        ports:
        - name: {{ .Values.whoami.ports.http.name }}
          containerPort: {{ .Values.whoami.ports.http.targetPort }}
{{- end }}
