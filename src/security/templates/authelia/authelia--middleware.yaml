apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: {{ .Values.k8s.names.middlewares.prefix }}{{ .Values.authelia.label }}{{ .Values.k8s.names.middlewares.suffix }}
  labels:
    chartVersion: {{ .Chart.Version | replace "+" "_" }}
    env: {{ .Values.project.env }}

spec:
  forwardAuth:
    address: 'http://{{ .Values.k8s.names.services.prefix }}{{ .Values.authelia.label }}{{ .Values.k8s.names.services.suffix }}:{{ .Values.authelia.ports.http.targetPort }}/api/authz/forward-auth'
    authResponseHeaders:
      - 'Remote-User'
      - 'Remote-Groups'
      - 'Remote-Email'
      - 'Remote-Name'
    trustForwardHeader: true
