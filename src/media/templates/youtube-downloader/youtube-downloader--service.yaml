apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.youtubeDownloader.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.youtubeDownloader.ports.http.name }}
    port: {{ .Values.youtubeDownloader.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.services.prefix }}{{ .Values.youtubeDownloader.label }}{{ .Values.k8s.names.pods.suffix }}
