youtubeDownloader:
  cookiesPath: /app/cookies.txt
  credentials:
    username: pedro
    password: mader
  host: ${k8s.names.urls.prefix}${youtubeDownloader.label}${k8s.names.urls.suffix}.${traefik.domain}
  image: local/histube:latest
  label: youtube-downloader
  ports:
    http:
      name: http
      targetPort: 8080
  pvc: ${storage.defaultVolumes.media}
  replicasNb: 1
