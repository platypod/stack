apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.rommapp.database.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.rommapp.database.ports.http.name }}
    port: {{ .Values.rommapp.database.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.services.prefix }}{{ .Values.rommapp.database.label }}{{ .Values.k8s.names.pods.suffix }}
