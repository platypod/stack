{{- if .Values.shelfmark.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.shelfmark.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.shelfmark.ports.http.name }}
    port: {{ .Values.shelfmark.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.shelfmark.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
