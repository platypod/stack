# mysql (dev-tools)

`bookstack-db` — the database backing [bookstack](bookstack.md).

- **Image:** `mysql:9` (pinned).
- **Scope:** internal only (ClusterIP); dedicated to BookStack.

Stock MySQL; credentials from the BookStack values.
