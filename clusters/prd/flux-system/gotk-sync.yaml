# This manifest was generated by flux. DO NOT EDIT — except spec.ref below,
# a deliberate one-time hand-edit (Phase 7 cutover): flux bootstrap always
# generates branch:main, but prod's steady state is a semver tag gate (see
# stack/docs/flux-migration.md's "prod pinned to a tag" decision). Re-running
# `make flux-bootstrap ENV=prd` (e.g. to pick up a new Flux version) will
# regenerate this file and silently revert spec.ref back to branch:main —
# re-apply this same edit after any future re-bootstrap.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    semver: ">=1.0.0"
  secretRef:
    name: flux-system
  url: ssh://git@github.com/platypod/stack
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/prd
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
