{{- if .Values.komga.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.komga.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.komga.ports.http.name }}
    port: {{ .Values.komga.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.komga.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
