apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.bookstack.names.service }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.bookstack.ports.http.name }}
    port: {{ .Values.bookstack.ports.http.targetPort }}
  selector:
    app: {{ .Values.bookstack.names.pod }}
