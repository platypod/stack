# This manifest was generated by flux. DO NOT EDIT.
#
# spec.ref is branch:main — flux bootstrap's own default. It used to be
# hand-edited to a semver tag gate (Phase 7 cutover: "prod pinned to a tag",
# see stack/docs/flux-migration.md) specifically so prod never saw a commit
# until a human deliberately cut a release. That gate is retired: prod now
# runs its own image-automation-controller (clusters/prd/image-automation.yaml)
# whose ImageUpdateAutomation only ever pushes to a branch, never a tag —
# for prod to see that commit at all, this GitRepository has to track
# branch:main, same as local. Consequence, not a side effect: any commit to
# stack's main (not just an image bump) now reaches prod within ~1 minute.
# A future re-bootstrap (`make flux-bootstrap ENV=prd`) regenerating this
# file to branch:main is no longer something to revert.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/platypod/stack
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/prd
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
