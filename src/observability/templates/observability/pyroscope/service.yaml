{{- if .Values.observability.profiles.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.observability.profiles.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.observability.profiles.ports.http.name }}
    port: {{ .Values.observability.profiles.ports.http.targetPort }}
    targetPort: {{ .Values.observability.profiles.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.observability.profiles.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
