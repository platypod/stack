# mariadb (games)

`rommapp-db` — the database backing [rommapp](rommapp.md).

- **Image:** `mariadb:11` (pinned).
- **Port:** `3306` (internal only).
- **Scope:** dedicated to RomM; no ingress.

Stock MariaDB; credentials from the rommapp values.
