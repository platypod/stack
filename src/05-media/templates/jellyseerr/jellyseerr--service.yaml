apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.jellyseerr.names.service }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.jellyseerr.ports.http.name }}
    port: {{ .Values.jellyseerr.ports.http.targetPort }}
  selector:
    app: {{ .Values.jellyseerr.names.pod }}
