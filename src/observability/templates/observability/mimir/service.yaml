{{- if .Values.observability.metrics.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.observability.metrics.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.observability.metrics.ports.http.name }}
    port: {{ .Values.observability.metrics.ports.http.targetPort }}
    targetPort: {{ .Values.observability.metrics.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.observability.metrics.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
