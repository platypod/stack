apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.radarr.names.service }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.radarr.ports.http.name }}
    port: {{ .Values.radarr.ports.http.targetPort }}
  selector:
    app: {{ .Values.radarr.names.pod }}
