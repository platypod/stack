homepage:
  host: ${k8s.names.urls.prefix}${homepage.label}${k8s.names.urls.suffix}.${traefik.domain}
  label: homepage
  image:
    registry: ghcr.io/
    namespace: gethomepage/
    repository: homepage
    tag: v0.10.4
  ports:
    http:
      name: homepage-http
      targetPort: 3000
