whoami:
  host: ${k8s.names.urls.prefix}${whoami.label}${k8s.names.urls.suffix}.${traefik.domain}
  image: traefik/whoami
  label: whoami
  ports:
    http:
      name: whoami-http
      targetPort: 80
  replicasNb: 2
