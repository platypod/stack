radarr:
  apiKey: 1b229188fe9e4488a2333c2135b1a053
  host: ${k8s.names.urls.prefix}${radarr.label}${k8s.names.urls.suffix}.${traefik.domain}
  image: linuxserver/radarr:latest
  label: radarr
  ports:
    http:
      name: radarr-http
      targetPort: 7878
  pvc:
    app: ${storage.defaultVolumes.apps}
    media: ${storage.defaultVolumes.media}
  replicasNb: 1
