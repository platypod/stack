youtubeDownloader:
  cookiesPath: /app/cookies.txt
  credentials:
    username: pedro
    password: mader
  host: "{{ .Values.k8s.names.urls.prefix }}{{ .Values.youtubeDownloader.label }}{{ .Values.k8s.names.urls.suffix }}.{{ .Values.traefik.domain }}"
  image: ghcr.io/pittinic/histube:latest
  label: youtube-downloader
  ports:
    http:
      name: http
      targetPort: 8080
  pvc: "{{ .Values.storage.defaultVolumes.media }}"
  replicasNb: 1
