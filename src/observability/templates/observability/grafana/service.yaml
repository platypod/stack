{{- if .Values.observability.dashboard.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.observability.dashboard.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.observability.dashboard.ports.http.name }}
    port: {{ .Values.observability.dashboard.ports.http.targetPort }}
    targetPort: {{ .Values.observability.dashboard.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.observability.dashboard.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
