media:
  system:
    userId: 501
    groupId: 20
    umask: 002
    data:
      path: /data
    downloads:
      ongoing: "{{ .Values.media.system.data.path }}/ongoing"
      finished: "{{ .Values.media.system.data.path }}/finished"
      torrents: "{{ .Values.media.system.data.path }}/torrents"
