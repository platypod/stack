apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.cyberChef.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.cyberChef.ports.http.name }}
    port: {{ .Values.cyberChef.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.cyberChef.label }}{{ .Values.k8s.names.pods.suffix }}
