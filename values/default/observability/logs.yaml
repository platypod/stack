observability:
  logs:
    label: loki
    # Loki tenant for logs that don't set X-Scope-OrgID (native Claude telemetry,
    # future k8s pod logs). Admins federate it (scopeShim.sharedTenants).
    defaultTenant: "_shared"
    image: grafana/loki:3.7.2
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        # Bumped from 512Mi — OOMKilled (exit 137) after auth_enabled +
        # multi-tenancy landed: per-tenant index/limits overhead plus
        # pattern_ingester's always-on metric_aggregation pushed it over.
        memory: 768Mi
    host: "{{ .Values.k8s.names.urls.prefix }}{{ .Values.observability.logs.label }}{{ .Values.k8s.names.urls.suffix }}.{{ .Values.traefik.domain }}"
    ports:
      grpc:
        name: grpc
        targetPort: 9096
      http:
        name: http
        targetPort: 3100
    logging:
      level: info
