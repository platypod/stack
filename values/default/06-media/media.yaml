media:
  system:
    userId: 501
    groupId: 20
    umask: 002
    data:
      path: /data
    downloads:
      ongoing: ${media.system.data.path}/ongoing
      finished: ${media.system.data.path}/finished
      torrents: ${media.system.data.path}/torrents
