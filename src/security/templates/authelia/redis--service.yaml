apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ tpl .Values.authelia.redis.label . }}{{ .Values.k8s.names.services.suffix }}

spec:
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ tpl .Values.authelia.redis.label . }}{{ .Values.k8s.names.pods.suffix }}
  ports:
  - name: {{ .Values.authelia.redis.ports.http.name }}
    port: {{ .Values.authelia.redis.ports.http.targetPort }}
    targetPort: {{ .Values.authelia.redis.ports.http.targetPort }}
