{{- if .Values.suwayomi.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.suwayomi.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.suwayomi.ports.http.name }}
    port: {{ .Values.suwayomi.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.suwayomi.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
