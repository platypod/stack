apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.homepage.names.service }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.homepage.ports.http.name }}
    port: {{ .Values.homepage.ports.http.targetPort }}
  selector:
    app: {{ .Values.homepage.names.pod }}
