{{- if .Values.outline.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ tpl .Values.outline.redis.label . }}{{ .Values.k8s.names.services.suffix }}

spec:
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ tpl .Values.outline.redis.label . }}{{ .Values.k8s.names.pods.suffix }}
  ports:
  - name: {{ .Values.outline.redis.ports.http.name }}
    port: {{ .Values.outline.redis.ports.http.targetPort }}
    targetPort: {{ .Values.outline.redis.ports.http.targetPort }}
{{- end }}
