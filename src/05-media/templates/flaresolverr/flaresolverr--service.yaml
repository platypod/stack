apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.flaresolverr.names.service }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.flaresolverr.ports.http.name }}
    port: {{ .Values.flaresolverr.ports.http.targetPort }}
  selector:
    app: {{ .Values.flaresolverr.names.pod }}
