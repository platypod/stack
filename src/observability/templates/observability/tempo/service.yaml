{{- if .Values.observability.traces.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.observability.traces.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.observability.traces.ports.grpc.name }}
    port: {{ .Values.observability.traces.ports.grpc.targetPort }}
    targetPort: {{ .Values.observability.traces.ports.grpc.targetPort }}
  - protocol: TCP
    name: {{ .Values.observability.traces.ports.http.name }}
    port: {{ .Values.observability.traces.ports.http.targetPort }}
    targetPort: {{ .Values.observability.traces.ports.http.targetPort }}
  - protocol: TCP
    name: {{ .Values.observability.traces.ports.otlpHttp.name }}
    port: {{ .Values.observability.traces.ports.otlpHttp.targetPort }}
    targetPort: {{ .Values.observability.traces.ports.otlpHttp.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.observability.traces.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
