{{- if .Values.whoami.enable }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.whoami.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.whoami.ports.http.name }}
    port: {{ .Values.whoami.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.whoami.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
