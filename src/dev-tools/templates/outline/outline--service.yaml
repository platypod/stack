{{- if .Values.outline.enabled }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.outline.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.outline.label }}{{ .Values.k8s.names.pods.suffix }}
  ports:
  - name: {{ .Values.outline.ports.http.name }}
    port: {{ .Values.outline.ports.http.targetPort }}
    targetPort: {{ .Values.outline.ports.http.targetPort }}
{{- end }}
