apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.dashy.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.dashy.label }}{{ .Values.k8s.names.pods.suffix }}
  ports:
  - name: {{ .Values.dashy.ports.http.name }}
    port: {{ .Values.dashy.ports.http.targetPort }}
    targetPort: {{ .Values.dashy.ports.http.targetPort }}
