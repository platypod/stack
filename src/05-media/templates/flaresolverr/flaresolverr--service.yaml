apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.flaresolverr.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.flaresolverr.ports.http.name }}
    port: {{ .Values.flaresolverr.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.services.prefix }}{{ .Values.flaresolverr.label }}{{ .Values.k8s.names.pods.suffix }}
