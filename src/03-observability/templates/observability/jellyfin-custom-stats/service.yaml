apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.observability.jellyfinCustomStats.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.observability.jellyfinCustomStats.ports.http.name }}
    port: {{ .Values.observability.jellyfinCustomStats.ports.http.targetPort }}
    targetPort: {{ .Values.observability.jellyfinCustomStats.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.observability.jellyfinCustomStats.label }}{{ .Values.k8s.names.pods.suffix }}
