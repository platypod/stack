# Historical reference only — local's real per-env values now live encrypted
# in platypod-sops/stack/local/secrets.enc.yaml (see stack/docs/flux-migration.md
# Phase 1), not a plain values/local/values.yaml. Kept as documentation of the
# shape that file's content takes.
acme:
  email: whoever@mail.com

k8s:
  names:
    urls:
      suffix: .${project.env}

project:
  env: local

traefik:
  domain: whatever.com
