# dbeaver / cloudbeaver (dev-tools)

CloudBeaver — web-based database client (DBeaver in the browser).

- **Image:** `dbeaver/cloudbeaver:24.3.5` (pinned).
- **Exposure:** host via Traefik, behind Authelia.
- **Storage:** workspace/config on a PVC.

Note: the backlog item about "version pinning except CloudBeaver" refers to CloudBeaver
historically being left on a moving tag — it is pinned here at `24.3.5`.
