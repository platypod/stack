{{- if .Values.rommapp.enabled }}
apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.k8s.names.services.prefix }}{{ .Values.rommapp.label }}{{ .Values.k8s.names.services.suffix }}

spec:
  ports:
  - protocol: TCP
    name: {{ .Values.rommapp.ports.http.name }}
    port: {{ .Values.rommapp.ports.http.targetPort }}
  selector:
    app: {{ .Values.k8s.names.pods.prefix }}{{ .Values.rommapp.label }}{{ .Values.k8s.names.pods.suffix }}
{{- end }}
